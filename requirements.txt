# FireSight-IR — runtime dependencies
# Tested against Python 3.10–3.12 on Google Colab and on local Linux/macOS.
# Pin versions if you need bit-reproducibility; for general use the
# loose constraints below should be safe.

# ── Core scientific stack ────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# ── Deep learning ────────────────────────────────────────────────────────────
torch>=2.1
# (CUDA wheel pinning is environment-specific. On Colab T4, pip installs the
# matching wheel automatically.)

# ── Data formats ─────────────────────────────────────────────────────────────
h5py>=3.9
pyarrow>=14.0      # parquet I/O
netCDF4>=1.6       # ERA5 NetCDF files
xarray>=2023.10    # for ERA5 / atmospheric data
rioxarray>=0.15    # MODIS GeoTIFF land cover

# ── Geospatial ───────────────────────────────────────────────────────────────
rasterio>=1.3
geopandas>=0.14
cartopy>=0.22      # publication maps (requires libgeos / libproj)
shapely>=2.0

# ── External-data clients ────────────────────────────────────────────────────
earthaccess>=0.7   # NASA earthdata (MODIS, VIIRS)
cdsapi>=0.7        # ERA5 from Copernicus CDS

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib>=3.8

# ── Tabular ML baselines (optional) ──────────────────────────────────────────
xgboost>=2.0
lightgbm>=4.0

# ── Dashboard (optional) ─────────────────────────────────────────────────────
streamlit>=1.30

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm>=4.66
